[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd /app/apps/api && npx prisma generate && npx prisma migrate deploy && node dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

