[phases.setup]
nixPkgs = ['nodejs_20']
aptPkgs = []

[phases.install]
cmds = [
  'npm install -g pnpm@9',
  'cd /app && pnpm install --frozen-lockfile'
]

[phases.build]
cmds = [
  'cd /app/apps/client && pnpm build'
]

[start]
cmd = 'cd /app/apps/client && pnpm start'

